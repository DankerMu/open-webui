# Tasks

Issue #7 / Plan 1 group 3, tasks 3.2–3.3.

Selected: Public API; Auth; Error handling; Concurrency; Schema/field names; Documentation.

Not selected: Config; File IO; Legacy; Release.

- [x] 3.2 Routes in `backend/open_webui/routers/ocu_workspaces.py` (auth route stays). Runtime: if `ENABLE_OCU_WORKSPACE` is false, the four workspace endpoints 404 (never 403) without calling owner/client. If true: `GET /workspaces/{chat_id}`, `POST /workspaces/{chat_id}/launch`, `POST /workspaces/{chat_id}/refresh`, `PUT /workspaces/{chat_id}/prefs`. Owner-only: non-owner and missing chat 404; anonymous 401 via `get_verified_user`; invalid chat ids 404 and `is_chat_owner` + client uncalled. Describe: stub/real `OcuClient.describe`; D7 map (`running`→running; other existing/absent+meta→stopped; `never_created`→unavailable/`never_created`; `OcuUnreachable`→200 unavailable/`ocu_unreachable` `views:[]` cursor unchanged); `capabilities`: `launch` iff stopped, `refresh` iff OCU answered, `prefs` always; `revision` = broker value and `OcuChatStates.advance_cursor` when OCU answered (including stopped); cursor only on unreachable; `base_url` `/ocu`; `cli_badge` from describe; no file list; never call launch. Launch: require `X-Requested-With: ocu-workspace` after auth before owner (403, client and `is_chat_owner` uncalled); `OcuNeverCreated` → 409 `never_created`; success 200. Refresh: same header; `OcuClient.refresh`; return revision; advance cursor; in-process token bucket 1 per 2 s burst 3 → 429 + `Retry-After`. Prefs: same header; body `{view?, selected_file_id?, open?}` ≤ 2 KiB; unknown keys or oversize 422; `OcuChatStates.upsert_prefs`. Inject the client so tests stub it (module-level factory or constructor the tests can monkeypatch). CONTEXT.md: Public Interfaces row lists the five `/api/v1/ocu/*` routes and 401/403/404/409/422; Core Invariants credential line adopts D13 (keys/token reach only the owning chat's sandbox, never the browser). Verify: module imports; `make doc-gate`.
- [x] 3.3 `backend/open_webui/test/routers/test_ocu_workspaces.py` (TestClient, harness DB, real `get_verified_user`, stub client; import `open_webui` inside helpers like `test_ocu_auth.py`): describe never calls launch; stopped/paused → `stopped` with `launch` in capabilities; running → no `launch`; unreachable → `capabilities == ["prefs"]`; `OcuUnreachable` → 200 unavailable/`ocu_unreachable` with stored cursor; launch/refresh/prefs with session without `X-Requested-With` → 403, `is_chat_owner` and client uncalled; same without session → 401; flag off → 404 for all four; launch 409 on never_created; refresh burst 3/5 → 429; cursor 5 vs broker 7 container stopped → returns 7 stores 7; prefs owner 200 / non-owner 404 / anonymous 401 / unknown key 422 / oversize 422; branch/clone has no state (new chat_id get is None until prefs/describe-advance); deleted chat → 404. Tests red against missing routes, then green. Verify: `cd backend && uv run pytest -q -p no:cacheprovider open_webui/test/routers/test_ocu_workspaces.py`; `make lint-scoped`; `make coverage-gate`; `make test-backend`.

Non-goals: smoke (#8); UI; OCU launch.
