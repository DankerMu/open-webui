# Tasks

## 1. Permanent gateway judge

- [x] 1.1 Approve expanded fixture and strict validation; bind source pin and user-authorized paired merge gate.
- [x] 1.2 Extend existing stub with private observations and required deterministic routes, remove public credential echo; preserve make smoke-stub and prove leaked-response RED followed by containment GREEN.
- [x] 1.3 Add smoke/proxy Hurl matrix and minimal supporting assertions for real owner/nonowner auth, active direct-WebUI alternate-header positive control, no-contact denials, mutation/upstream status provenance, identity/token receipt, prefix/encoding/upload/WS and adversarial file policy; qualify missing-auth/wrong-route/credential-echo faults in isolated copies.
- [x] 1.4 Add finite make smoke-proxy lifecycle and exact-checkout verification with isolated byte-identical proxy copy; prove missing prerequisite/wrong SHA fails loudly, original ignored config/runtime remain untouched, and failure/interruption cleans only owned services/data.

## 2. CI and paired acceptance

- [x] 2.1 Add constraints.yaml exact OCU pin and CI layer3 checkout/native nginx/smoke step; update AGENTS.md and constraints Verification Matrix, flag CI changes and verify doc-gate/decisions-verify.
- [x] 2.2 Parent runs make smoke-proxy locally against pinned sibling, make smoke-stub and ordinary make smoke without proxy dependency; capture exact source/command/exit and sanitized runtime proof, no Docker.
- [x] 2.3 Expanded auth/integration, harness lifecycle/evidence and CI/security review with bounded repairs; require exact paired SHA CI success before sequential OCU then WebUI merge and both fixture archives.
